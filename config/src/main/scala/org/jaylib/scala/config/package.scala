package org.jaylib.scala

/**
 * The main package for all configuration related packages.
 * It also provides the interface [[ObservableConfig]] to derive actual configuration traits from to
 * get observable configurations.
 */
package object config {
}